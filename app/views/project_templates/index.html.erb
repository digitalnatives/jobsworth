<% @page_title = "Project Templates - #{Setting.productName}" %>

<% content_for :navigation do %>
  <%= render :partial => "companies/navigation", :locals => {:selected => "project_templates"} %>
<% end %>

<legend>
  Project Templates
  <%= link_to t('project_templates.new_template'), new_project_template_path, :class => "btn pull-right" %>
</legend>

<ul>
  <% @project_templates.each do |template| %>
    <li>
      <%= link_to template, edit_project_template_path(template) %>
      <div style="display:inline;" class="task-action">
        <%= link_to('<i class="icon-remove"></i>'.html_safe, project_template_path(template), :method => :delete,
            :confirm => t('shared.are_you_sure'), :title => t('project_templates.delete', model: template.name ), :rel => "tooltip") %>
      </div>
    </li>
  <% end %>
</ul>

